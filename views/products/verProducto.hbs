<header>
  <nav class="navbar navbar-expand-lg fixed-top">
    <div class="container">
      <a class="navbar-brand" href="/"><img src="/imagenes/delimar.png" class="logo-brand" alt="logo"></a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <ion-icon name="menu-outline"></ion-icon>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <a class="nav-link" href="/" id="inicio">Inicio</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/lista_producto" id="catalogo">Productos</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#" id="info">Nosotros</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#" id="contacto">Contáctanos</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</header>

<div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center" style="padding: 100px 0;">
  <h1 class="display-4">{{dataValues.name}}</h1>
</div>

{{!-- Mostrar los mensajes en caso de que existan --}}
{{#if errors}}
  {{#each errors}}
    <div class="alert {{type}}" role="alert">
      {{error}}
    </div>
  {{/each}}
{{/if}}
<div class="card mb-3" style="max-width: 540px;">
  <div class="row no-gutters">
    <div class="col-md-4">
      <img src="{{dataValues.imagepath}}" class="card-img" alt="{{dataValues.name}}">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title">{{dataValues.name}}</h5>
        <p class="card-text">{{dataValues.description}}</p>
        <p class="card-text"><small class="text-muted">${{dataValues.price}}</small></p>
        <p class="card-text"><small class="text-muted">Existencias: {{dataValues.libra}} libra(s)</small></p>
      </div>
    </div>
  </div>
</div>
